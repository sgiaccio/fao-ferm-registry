rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // if user is signed in and he has been assigned at least to one group
      allow read, write: if request.auth != null && request.auth.token.privileges.size() > 0;
    }
  }
}
